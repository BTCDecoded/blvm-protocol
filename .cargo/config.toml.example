# Example .cargo/config.toml for local development
# Copy this file to .cargo/config.toml to use local blvm-consensus path

[build]
# Use local path replacement for development
[replace]
"blvm-consensus:0.1.6" = { path = "../blvm-consensus" }
